services:
  evidence-desk:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      REPORT_DATA_DIR: "/data"
      REPORT_MAX_BYTES: "10485760"
    volumes:
      - paper-review-data:/data

volumes:
  paper-review-data:
